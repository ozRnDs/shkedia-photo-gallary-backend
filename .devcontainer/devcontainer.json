{
    "build": {
        "dockerfile": "Dockerfile",
        "context": "../"
    },
    "forwardPorts": [8000],
    "runArgs": [ "-v", "${localEnv:POSTGRES_CRED_LOCATION}/:/temp/", "-p","24500:8000"],
    "shutdownAction": "stopContainer"
}